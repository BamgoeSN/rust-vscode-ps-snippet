fn lcm<T>(x: T, y: T) -> T
where
    T: Copy
        + PartialEq
        + PartialOrd
        + std::ops::Rem<Output = T>
        + std::ops::Div<Output = T>
        + std::ops::Mul<Output = T>
        + From<u8>,
{
    x / gcd(x, y) * y
}
